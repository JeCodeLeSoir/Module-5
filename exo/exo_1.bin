=== Categories ===
Exercice
=== Titre ===
Exercice 1
=== Enonce ===
Modifier le code ci-dessous pour calculer la moyenne des
notes.

<?php
$note_maths = 15;
$note_francais = 12;
$note_histoire_geo = 9;
$moyenne = 0;
echo 'La moyenne est de '.$moyenne.' / 20.';
?>

=== Code ===
<?php

if(isset($_POST['notes'])){
    $notes = explode(",", $_POST['notes']);

    $moyenne = 0;

    for ($i = 0; $i < count($notes); $i++) {
        $notes[$i] = intval($notes[$i]);
        $moyenne += $notes[$i];
    }

    $moyenne = $moyenne / count($notes);
    
    echo "La moyenne est de ".$moyenne."/20.\n";
}

?>

<form method="POST">
    <input type="text" name="notes" value="15, 12, 9">
    <input type="submit" value ="Calculer">
</form>

=== Exo ===
 >== Param
[15, 12, 9]
 >== Result
12